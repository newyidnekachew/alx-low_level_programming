#include "main.h"
/**
 *_abs - calculate the absolute value of the number from zero.
 *@n: the int to check
 *Return: the absolute value of int
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
